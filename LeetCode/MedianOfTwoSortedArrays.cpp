/*
There are two sorted arrays nums1 and nums2 of size m and n respectively.
Find the median of the two sorted arrays.
The overall run time complexity should be O(log (m+n)).
*/

/*#include <iostream>
#include "MedianOfTwoSortedArrays.h"
using namespace std;

int main() {
	int ary1[4] = { 2, 5, 15, 20 };
	int ary2[7] = { 1, 3, 5, 8, 10, 13, 18 };

	vector<int> vi1(ary1, ary1 + sizeof(ary1) / sizeof(int));
	vector<int> vi2(ary2, ary2 + sizeof(ary2) / sizeof(int));

	cout << Solution::findMedianSortedArrays(vi1, vi2) << endl;
	return 0;
}*/